<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- MySQL Dialect -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQLDialect
        </property>

        <!-- MySQL Driver -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <!-- MySQL Connection URL -->
        <property name="hibernate.connection.url">
            jdbc:mysql://mysql-c7d22b5-huymaidang-1cc4.d.aivencloud.com:19257/sba301_sp26?useSSL=true&amp;requireSSL=true&amp;verifyServerCertificate=false&amp;allowPublicKeyRetrieval=true&amp;serverTimezone=UTC
        </property>

        <property name="hibernate.connection.username">
            username
        </property>

        <property name="hibernate.connection.password">
            password
        </property>

        <!-- Optional Hibernate Settings -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Connection Pool (built-in) -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- Performance -->
        <property name="hibernate.jdbc.batch_size">50</property>
        <property name="hibernate.order_inserts">true</property>
        <property name="hibernate.order_updates">true</property>

        <!-- Timezone handling -->
        <property name="hibernate.jdbc.time_zone">UTC</property>

        <!-- Mapping Classes -->
        <mapping class="org.example.entity.Book"/>
        <mapping class="org.example.entity.Student"/>

    </session-factory>
</hibernate-configuration>